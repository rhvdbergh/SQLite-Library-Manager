h1= title
p
  a.button(href='new_book.html') Create New Book
p
  | Filters 
  a(href='all_books.html') All 
  | | 
  a(href='overdue_books.html') Overdue 
  | | 
  a(href='checked_books.html') Checked Out 
form(action='get')
  p
    input#search_box(type='text' name='search_term' placeholder='Search ...')
table
  thead
    tr
      th Book Title
      th Author
      th Genre
      th Year Released (First Published)
  tbody
    each book in books
      tr
        td
          a(href=`book/${book.dataValues.id}`) #{book.dataValues.title}
        td #{book.dataValues.author}
        td #{book.dataValues.genre}
        td #{book.dataValues.first_published}
include pagination.pug